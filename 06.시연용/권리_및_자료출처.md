# 자료 출처와 이용 안내

이 폴더는 저장소 관리자가 제공한 `Daechung_Demo20_GT_release_v1`에서 원본 사진20장과 JSON20개(검수 라벨629개)만 포함합니다. 사진·라벨 파일의 내용은 변경하지 않았으며, 시연용CSV·이미지는 이 라벨에서 생성했습니다. 모델의 자동 탐지 결과는 `03.모델예측/`에 별도로 구분합니다.

- 저장소 자체 코드의 라이선스는 루트의 [MIT License](../LICENSE)를 따릅니다.
- 사진·라벨·파생 산출물·OBJ·모델 가중치가 코드의 MIT 라이선스로 자동 재허가되는 것은 아닙니다. 재배포·다른 목적으로의 사용 조건은 제공자에게 확인하십시오. 별도의 영상 라이선스 문서는 제공 자료에서 확인되지 않았습니다.
- SAM3와 그 파생 모델은 [SAM License](../02_Model/sam3_aug512/third_party/SAM_LICENSE)의 적용 여부와 조건을 확인해야 합니다. SAM3는 별도로 설치하며 체크포인트는 이 Git 폴더에 포함하지 않습니다.
- 루트 `THIRD_PARTY_NOTICES.md`의 기존 배포 자료에 관한 “no ground truth” 설명과 달리, **이번 `06.시연용`에는 검수된 정답 라벨과 그 파생 산출물을 명시적으로 추가했습니다.**
- 학습·정량 기록에 남은 과거 머신 경로는 실행 근거입니다. 플랫폼에서 사용하는 CSV 이미지 경로는 모두 산출물 폴더 기준 상대경로입니다.

이 자료는 시연용입니다. 정량값은 근사치이며 실제 측량·균열 개구폭 측정이나 구조 안전 판정을 대신하지 않습니다.
