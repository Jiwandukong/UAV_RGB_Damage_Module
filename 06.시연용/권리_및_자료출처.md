# 자료 출처와 이용 안내

원본 자료는 관리자가 제공한 `Daechung_Demo20_GT_release_v1`의 사진 20장과 JSON 20개(검수 라벨 629개)입니다. 사진·라벨 파일의 내용은 변경하지 않았습니다.

- `04.시연산출물/`의 CSV·라벨 오버레이는 검수 라벨 기준 결과입니다.
- `04.시연산출물/모델추론결과/`는 원본 사진을 모델로 추론한 별도 결과입니다. 추론 중 라벨은 사용하지 않았지만, 이 시연 모델의 학습에 사용한 사진이므로 일반화 성능 평가 자료가 아닙니다.
- 자체 코드와 이 폴더에 포함된 기존 좌표·정량 모듈은 [MIT License](LICENSE)를 따릅니다.
- 사진·라벨·파생 산출물·OBJ·모델 가중치가 코드의 MIT 라이선스로 자동 재허가되는 것은 아닙니다. 재배포·다른 목적으로의 사용 조건은 제공자에게 확인하십시오. 별도의 영상 라이선스 문서는 제공 자료에서 확인되지 않았습니다.
- SAM3와 그 파생 모델은 [SAM License](third_party/SAM_LICENSE)의 적용 여부와 조건을 확인해야 합니다. 공식 SAM3 패키지는 별도로 설치하며 체크포인트와 OBJ는 Release에서 받습니다.
- 학습·정량 기록에 남은 과거 머신 경로는 실행 근거입니다. 플랫폼에서 사용하는 CSV 이미지 경로는 모두 산출물 폴더 기준 상대경로입니다.

이 자료는 시연용입니다. 정량값은 근사치이며 실제 측량·균열 개구폭 측정이나 구조 안전 판정을 대신하지 않습니다.
